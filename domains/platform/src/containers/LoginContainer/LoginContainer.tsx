import { LoginForm } from '@/shared'

// Styling
import { Layout } from './LoginContainer.styled'

export function LoginContainer() {
    return (
        <Layout.Root>
            <LoginForm />
        </Layout.Root>
    )
}
